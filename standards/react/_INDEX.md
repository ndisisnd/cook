<!-- AUTO-GENERATED from SKILL.md frontmatter — do not edit manually -->
# react Skills Index

## File Match

| Skill | File pattern | Keywords |
| ----- | ------------ | -------- |
| **react** | `**/*.tsx`, `**/*.jsx` | component, props, children, JSX, hooks, useEffect, useState, useCallback, useMemo, useRef, useContext, useReducer, ReactNode |
| react → testing ref | `**/*.test.tsx`, `**/*.spec.tsx`, `**/*.test.jsx`, `**/*.spec.jsx` | render, screen, userEvent, MSW, mock service worker |
| react → tooling ref | `package.json`, `vite.config.*`, `.eslintrc.*`, `eslint.config.*` | vite, webpack, bundle, StrictMode, profile, why-did-you-render |
| react → state-management ref | `**/store/**`, `**/stores/**` | zustand, redux, RTK, react-query, SWR, store |
| react → performance ref | `**/*.tsx`, `**/*.jsx` | waterfall, lazy, Suspense, dynamic, virtualize, react-window, bundle size |
| react → security ref | `**/*.tsx`, `**/*.jsx` | dangerouslySetInnerHTML, XSS, DOMPurify, JWT, auth, CSP, CSRF, sanitize |
| react → component-patterns ref | `**/*.tsx`, `**/*.jsx` | compound component, HOC, render prop, composition, controlled, uncontrolled |

## Loading Instructions

> Load `<SKILLS>/react/SKILL.md` for any `.tsx` or `.jsx` file — covers hook correctness (P0), component basics (P0), boundary safety (P0), TypeScript conventions (P1), and style conventions (P1).
>
> Load `<SKILLS>/react/refs/testing.md` when touching test files or writing RTL/MSW tests.
>
> Load `<SKILLS>/react/refs/tooling.md` when touching Vite/webpack config, ESLint config, or bundle tooling.
>
> Load `<SKILLS>/react/refs/state-management.md` when choosing or implementing a state solution.
>
> Load `<SKILLS>/react/refs/performance.md` when investigating re-renders, waterfalls, or bundle bloat.
>
> Load `<SKILLS>/react/refs/security.md` when touching auth, XSS, CSRF, or CSP concerns.
>
> Load `<SKILLS>/react/refs/component-patterns.md` when designing reusable, composable component APIs.

## Deprecated (pending removal)

The following sub-skill folders have been merged into `SKILL.md` and `refs/`:

- `react-component-patterns/` → merged into `refs/component-patterns.md`
- `react-hooks/` → merged into `SKILL.md` (P0 hook correctness)
- `react-performance/` → merged into `refs/performance.md`
- `react-security/` → merged into `refs/security.md`
- `react-state-management/` → merged into `refs/state-management.md`
- `react-testing/` → merged into `refs/testing.md`
- `react-tooling/` → merged into `refs/tooling.md`
- `react-typescript/` → merged into `SKILL.md` (P1 TypeScript)
